# Isolated test environment only. Never point this at production.
AMT_TEST_BASE_URL=http://127.0.0.1:18010
AMT_TEST_EMAIL=admin-test@example.com
AMT_TEST_PASSWORD=REPLACE_WITH_TEST_PASSWORD

# Backend test instance should use a separate DB, for example:
# DB_NAME=amt_database_test
